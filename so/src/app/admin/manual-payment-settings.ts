import {Component,inject,signal} from '@angular/core';import {HttpClient} from '@angular/common/http';import {FormsModule} from '@angular/forms';import {finalize,switchMap} from 'rxjs';
interface ManualSettings{payPalRecipient:string;payPalName:string;bitPhone:string;bitName:string;version:number;}
@Component({selector:'app-manual-payment-settings',standalone:true,imports:[FormsModule],styleUrl:'./admin.css',template:`<section class="inventory-row" dir="rtl"><h3>תשלום ידני — PayPal ו־bit</h3><p>פרטים אלה יופיעו ללקוחות. השארת חשבון ריק משביתה את האפשרות. שינויים יחולו רק על הזמנות חדשות.</p>@if(settings();as s){<form #f="ngForm" (ngSubmit)="save()"><fieldset [disabled]="busy()"><legend>PayPal אישי</legend><label>מייל או קישור paypal.me<input name="paypal" [(ngModel)]="s.payPalRecipient" maxlength="254" dir="ltr"></label><label>שם המקבל ב־PayPal<input name="paypalName" [(ngModel)]="s.payPalName" maxlength="100"></label></fieldset><fieldset [disabled]="busy()"><legend>bit אישי</legend><label>מספר לקבלת תשלום<input name="bitPhone" [(ngModel)]="s.bitPhone" maxlength="10" inputmode="tel" dir="ltr"></label><label>שם המקבל ב־bit<input name="bitName" [(ngModel)]="s.bitName" maxlength="100"></label></fieldset><p>המערכת אינה בודקת את ההעברה מול השירותים. יש לבדוק שהסכום המלא התקבל ולאשר בהזמנה.</p><button [disabled]="busy()||f.invalid">שמירת פרטי התשלום</button></form>}<p role="status">{{message()}}</p></section>`})
export class ManualPaymentSettings{private http=inject(HttpClient);settings=signal<ManualSettings|null>(null);busy=signal(false);message=signal('');constructor(){this.http.get<ManualSettings>('/api/admin/manual-payments').subscribe({next:s=>this.settings.set(s),error:()=>this.message.set('לא ניתן לטעון פרטי תשלום.')});}save(){if(this.busy())return;this.busy.set(true);this.http.get<{token:string}>('/api/admin/csrf').pipe(switchMap(({token})=>this.http.put<ManualSettings>('/api/admin/manual-payments',this.settings(),{headers:{'X-CSRF-TOKEN':token}})),finalize(()=>this.busy.set(false))).subscribe({next:s=>{this.settings.set(s);this.message.set('פרטי התשלום נשמרו.');},error:e=>this.message.set((e.status===0?null:e.error?.message)||'לא ניתן לשמור. רעננו ובדקו את הפרטים.')});}}
