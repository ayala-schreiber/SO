import {Component,inject,signal} from '@angular/core';
import {HttpClient} from '@angular/common/http';
interface Readiness {activeProducts:number;invalidProducts:number;businessDetailsConfigured:boolean;contactConfigured:boolean;manualPaymentConfigured:boolean;mailConfigured:boolean;pendingMigrations:number;environment:string;}
@Component({selector:'so-launch-readiness',standalone:true,styleUrl:'./launch-readiness.css',template:`
<section aria-label="מוכנות לפתיחת החנות"><h2>לקראת פתיחת החנות</h2><p>הבדיקה מציגה הגדרות קיימות. היא אינה אישור לפתיחה לציבור.</p>
@if(data();as r){<ul><li><strong>מוצרים ומלאי</strong><span>{{r.activeProducts}} מוצרים פעילים · {{r.invalidProducts?'נדרש תיקון ב־'+r.invalidProducts+' מוצרים':'פרטי הבסיס מלאים'}}</span><small>יש לאמת ספירת מלאי, מחירים ותמונות בפועל.</small></li><li><strong>פרטי העסק והקשר</strong><span>{{r.businessDetailsConfigured&&r.contactConfigured?'פרטים הוזנו — נדרש אימות':'נותרו פרטים להשלמה בטופס'}}</span></li><li><strong>העברה ידנית</strong><span>{{r.manualPaymentConfigured?'פרטי העברה הוגדרו':'יש להשלים פרטי מקבל תשלום'}}</span><small>אישור הכסף נעשה ידנית, לאחר בדיקה בחשבון.</small></li><li><strong>איפוס סיסמה במייל</strong><span>{{r.mailConfigured?'הגדרות קיימות — נדרשת בדיקת מסירה':'שירות מייל טרם הוגדר'}}</span></li><li><strong>סביבת האתר</strong><span>{{r.environment==='Local'?'סביבה מקומית — האתר טרם פורסם':'מצב ייצור — יש לבדוק דומיין, HTTPS וגיבויים'}}</span>@if(r.pendingMigrations){<small>ממתינים {{r.pendingMigrations}} עדכוני מסד.</small>}</li></ul><p>לפני פתיחה: בדיקת נגישות ומדיניות, שחזור גיבוי ובדיקת רכישה מלאה בסביבת הפרסום. SMS אוטומטי אינו פעיל.</p>}
@if(error()){<p role="status">לא ניתן לטעון את בדיקת המוכנות.</p>}<button type="button" (click)="load()" [disabled]="loading()">בדיקה מחדש</button></section>`})
export class LaunchReadiness {private http=inject(HttpClient);data=signal<Readiness|null>(null);error=signal(false);loading=signal(false);constructor(){this.load();}load(){this.loading.set(true);this.error.set(false);this.http.get<Readiness>('/api/admin/readiness').subscribe({next:r=>{this.data.set(r);this.loading.set(false);},error:()=>{this.error.set(true);this.loading.set(false);}});}}
