import {IsraeliPhoneValidator,normalizePhone,displayPhone} from '../services/israeli-phone';
import {ManualPaymentInstructions} from './manual-payment-instructions';
import {ManualMethod} from '../services/payment-labels';
import {Component,inject,signal,viewChild,ElementRef} from '@angular/core';
import {FormsModule,NgForm} from '@angular/forms';
import {RouterLink} from '@angular/router';
import {HttpClient} from '@angular/common/http';
import {finalize} from 'rxjs';
import {CartService} from '../services/cart';
import {CustomerApi,Order} from '../services/customer';
import {ShippingSettings,deliveryFee} from '../services/shipping';
@Component({standalone:true,imports:[FormsModule,RouterLink,ManualPaymentInstructions,IsraeliPhoneValidator],styleUrls:['../admin/admin.css','./manual-payment.css'],template:`
<section class="admin-panel customer-panel" dir="rtl">
<nav class="checkout-progress" aria-label="שלבי ההזמנה"><ol>@for(label of steps;track $index){<li [class.done]="step()>$index+1" [class.current]="step()===$index+1" [attr.aria-current]="step()===$index+1?'step':null"><span class="step-dot">{{step()>$index+1?'✓':$index+1}}</span><span>{{label}}</span></li>}</ol><div class="step-track" aria-hidden="true"><span [style.width.%]="(step()-1)*50"></span></div></nav>
<h1 #stepTitle tabindex="-1" id="checkout-title">{{steps[step()-1]}}</h1>
@if(saved();as o){<h3>הזמנה {{o.number}}</h3><app-manual-payment-instructions [order]="o" [busy]="busy()" (declared)="declarePaid(o)" /><a [routerLink]="['/orders',o.id]">לפרטי ההזמנה</a>}
@else if(!cart.cartItems().length){<p>הסל שלך ריק.</p>}
@else {
@if(!cart.canCheckout()&&!cart.busy()){<p role="status">יש לבדוק את הזמינות ולעדכן את הסל לפני המשך להזמנה.</p>}
<p class="required-note"><span class="required-mark" aria-hidden="true">*</span> שדה חובה</p><form #f="ngForm" (ngSubmit)="step()===1?next(f):submit()">
<div class="step-details" [hidden]="step()!==1">
@if(!api.session()){<p><a routerLink="/account">כניסה לחשבון</a> או המשך כאורחת</p>}
<label><span id="label-name">שם מלא <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-name" #nameField="ngModel" [attr.aria-invalid]="nameField.touched&&nameField.invalid?true:null" [attr.aria-describedby]="nameField.touched&&nameField.invalid?'error-name':null" name="name" [(ngModel)]="name" required maxlength="150" autocomplete="name">@if(nameField.touched&&nameField.invalid){<small class="field-error" id="error-name">יש להזין שם מלא.</small>}</label>
<label><span id="label-email">מייל <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-email" #emailField="ngModel" [attr.aria-invalid]="emailField.touched&&emailField.invalid?true:null" [attr.aria-describedby]="emailField.touched&&emailField.invalid?'error-email':null" name="email" [(ngModel)]="email" type="email" email required maxlength="254" autocomplete="email">@if(emailField.touched&&emailField.invalid){<small class="field-error" id="error-email">יש להזין כתובת מייל תקינה.</small>}</label>
<label><span id="label-phone">טלפון <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-phone" #phoneField="ngModel" [attr.aria-invalid]="phoneField.touched&&phoneField.invalid?true:null" [attr.aria-describedby]="phoneField.touched&&phoneField.invalid?'error-phone':null" name="phone" [(ngModel)]="phone" type="tel" soIsraeliPhone required maxlength="20" autocomplete="tel" dir="ltr" (blur)="phone=displayPhone(phone)">@if(phoneField.touched&&phoneField.invalid){<small class="field-error" id="error-phone">יש להזין טלפון ישראלי תקין: 10 ספרות לנייד או 9 ספרות לקו נייח.</small>}</label>
@if(shipping();as s){<fieldset class="payment-fieldset"><legend>קבלת ההזמנה</legend><div class="payment-options"><label class="payment-choice"><input type="radio" name="delivery" [value]="false" [(ngModel)]="pickup" (ngModelChange)="quote.set(null)">משלוח</label>@if(s.pickupEnabled){<label class="payment-choice"><input type="radio" name="delivery" [value]="true" [(ngModel)]="pickup" (ngModelChange)="quote.set(null)">איסוף עצמי</label>}</div></fieldset>@if(pickup){<p>{{s.pickupAddress}} · ללא עלות</p><p>האיסוף בתיאום מראש בלבד</p>@if(s.pickupWhatsAppUrl){<a [href]="s.pickupWhatsAppUrl" target="_blank" rel="noopener noreferrer">לתיאום האיסוף ב־WhatsApp</a>}}@else{<p>אספקה בתוך 10 ימי עסקים.</p><div class="address-fields"><label class="address-wide"><span id="label-city">עיר <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-city" #cityField="ngModel" [attr.aria-invalid]="cityField.touched&&cityField.invalid?true:null" [attr.aria-describedby]="cityField.touched&&cityField.invalid?'error-city':null" name="city" [(ngModel)]="city" required maxlength="100" autocomplete="address-level2">@if(cityField.touched&&cityField.invalid){<small class="field-error" id="error-city">יש להזין עיר.</small>}</label><label class="address-wide"><span id="label-street">רחוב <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-street" #streetField="ngModel" [attr.aria-invalid]="streetField.touched&&streetField.invalid?true:null" [attr.aria-describedby]="streetField.touched&&streetField.invalid?'error-street':null" name="street" [(ngModel)]="street" required maxlength="150" autocomplete="address-line1">@if(streetField.touched&&streetField.invalid){<small class="field-error" id="error-street">יש להזין רחוב.</small>}</label><label><span id="label-houseNumber">מספר בית <span class="required-mark" aria-hidden="true">*</span></span><input aria-labelledby="label-houseNumber" #houseNumberField="ngModel" [attr.aria-invalid]="houseNumberField.touched&&houseNumberField.invalid?true:null" [attr.aria-describedby]="houseNumberField.touched&&houseNumberField.invalid?'error-houseNumber':null" name="houseNumber" [(ngModel)]="houseNumber" required maxlength="20" autocomplete="off">@if(houseNumberField.touched&&houseNumberField.invalid){<small class="field-error" id="error-houseNumber">יש להזין מספר בית.</small>}</label><label>דירה (לא חובה)<input name="apartment" [(ngModel)]="apartment" maxlength="20" autocomplete="address-line2"></label><label class="address-wide"><span>מיקוד (לא חובה)</span><input name="postalCode" [(ngModel)]="postalCode" maxlength="7" pattern="[0-9]{5}([0-9]{2})?" inputmode="numeric" autocomplete="postal-code" #postalField="ngModel" [attr.aria-invalid]="postalField.touched&&postalField.invalid?true:null" aria-describedby="postal-hint"><small id="postal-hint">5 או 7 ספרות</small></label><label class="address-wide"><span>הערות נוספות למשלוח (לא חובה)</span><textarea name="deliveryNotes" [(ngModel)]="deliveryNotes" maxlength="500" rows="3" placeholder="למשל: קוד כניסה לבניין או הנחיות הגעה"></textarea></label></div>}}</div>
@if(step()===2){
<div class="checkout-review"><span>{{name}}</span><span>{{pickup?'איסוף עצמי':address}}</span><button type="button" class="edit-details" (click)="go(1)">עריכת פרטים</button></div>
<div class="coupon-entry"><label for="coupon-code">קוד קופון</label><input id="coupon-code" [disabled]="quoteBusy()||busy()" name="couponCode" [(ngModel)]="couponCode" (ngModelChange)="quote.set(null);couponMessage.set('')" [attr.aria-invalid]="couponMessage()?true:null" [attr.aria-describedby]="couponMessage()?'coupon-error':null" maxlength="30" autocomplete="off"><button type="button" (click)="applyCoupon()" [disabled]="busy()||quoteBusy()">החלת קופון</button>@if(couponMessage()){<p class="field-error coupon-error" id="coupon-error" role="alert">{{couponMessage()}}</p>}</div>
<dl class="checkout-totals"><div><dt>מוצרים</dt><dd>{{cart.totalPrice()}} ₪</dd></div>@if(quote()?.discount){<div><dt>הנחה</dt><dd>{{quote()!.discount}} ₪</dd></div>}<div><dt>משלוח</dt><dd>{{deliveryTotal()}} ₪</dd></div><div class="grand-total"><dt>סה״כ</dt><dd>{{quote()?.total??(cart.totalPrice()+deliveryTotal())}} ₪</dd></div></dl>
<fieldset class="payment-fieldset"><legend>אמצעי תשלום</legend><div class="payment-options">@for(method of methods();track method.id){<label class="payment-choice"><input type="radio" name="paymentMethod" [value]="method.id" [(ngModel)]="paymentMethod" [disabled]="!method.available||busy()" required>{{method.label}}@if(!method.available){<small>לא זמין כרגע</small>}</label>}</div></fieldset><p class="checkout-note">העברה ידנית. פרטי התשלום יוצגו בשלב הבא.</p>
}
<button type="submit" [disabled]="busy()||quoteBusy()||!shipping()||!cart.canCheckout()||cart.busy()||!normalizePhone(phone)||(step()===2&&!paymentMethod)">{{busy()?'יוצרים הזמנה…':step()===1?'המשך לתשלום':'יצירת הזמנה והמשך להעברה'}}</button>
</form>}
@if(message()){<p role="status">{{message()}}</p>}
@if(!saved()){<a routerLink="/cart">חזרה לסל</a>}</section>`})
export class Checkout {
 displayPhone=displayPhone;normalizePhone=normalizePhone;
 steps=['פרטים ומשלוח','בחירת תשלום','העברה ואישור'];step=signal(1);stepTitle=viewChild<ElementRef<HTMLElement>>('stepTitle');
 go(step:number){this.step.set(step);this.message.set('');setTimeout(()=>this.stepTitle()?.nativeElement.focus());}
 next(form:NgForm){if(!this.cart.canCheckout())return;form.form?.markAllAsTouched();if(form.invalid||!this.name.trim()||!normalizePhone(this.phone)||!this.validAddress()){this.message.set('יש להשלים את הפרטים.');return;}this.go(2);}
 get address(){return this.pickup?'':[this.city.trim(),[this.street.trim(),this.houseNumber.trim()].filter(Boolean).join(' '),this.apartment.trim()?'דירה '+this.apartment.trim():''].filter(Boolean).join(', ');}
 validAddress(){return this.pickup||!!(this.city.trim()&&this.street.trim()&&this.houseNumber.trim()&&this.city.length<=100&&this.street.length<=150&&this.houseNumber.length<=20&&this.apartment.length<=20&&(!this.postalCode||/^[0-9]{5}([0-9]{2})?$/.test(this.postalCode))&&this.deliveryNotes.length<=500);}
 // כשיש הצעת מחיר מהשרת היא גוברת; אחרת מוצג אותו חישוב בדיוק כמו בעגלה.
 deliveryTotal(){return this.quote()?.deliveryFee??deliveryFee(this.shipping(),this.cart.totalPrice(),this.pickup);}

 methods=signal<ManualMethod[]>([]);paymentMethod='';
 cart=inject(CartService);api=inject(CustomerApi);private http=inject(HttpClient);shipping=signal<ShippingSettings|null>(null);busy=signal(false);message=signal('');saved=signal<Order|null>(null);name='';email='';phone='';city='';street='';houseNumber='';apartment='';postalCode='';deliveryNotes='';pickup=false;couponCode='';quote=signal<{discount:number;deliveryFee:number;total:number}|null>(null);quoteBusy=signal(false);couponMessage=signal('');
 applyCoupon(){if(this.quoteBusy()||this.busy())return;this.quote.set(null);this.quoteBusy.set(true);this.couponMessage.set('');this.api.post<any>('/api/orders/quote',{couponCode:this.couponCode,pickup:this.pickup,items:this.cart.cartItems().map(i=>({productId:i.product.id,quantity:i.quantity}))}).pipe(finalize(()=>this.quoteBusy.set(false))).subscribe({next:q=>this.quote.set(q),error:e=>this.couponMessage.set((e.status===0?null:e.error?.message)||'לא ניתן לבדוק את הקופון כרגע.')});}
 declarePaid(o:Order){if(this.busy())return;this.busy.set(true);this.message.set('');
  this.api.declarePaid(o.number).pipe(finalize(()=>this.busy.set(false))).subscribe({next:updated=>this.saved.set(updated),error:e=>this.message.set((e.status===0?null:e.error?.message)||'לא ניתן לעדכן כרגע. אפשר לסמן שוב או לפנות לחנות.')});}
 private retry:{fingerprint:string;key:string}|null=null;
 private retryReady:Promise<void>=Promise.resolve();
 private async fingerprint(body:string){return Array.from(new Uint8Array(await crypto.subtle.digest('SHA-256',new TextEncoder().encode(body)))).map(b=>b.toString(16).padStart(2,'0')).join('');}
 private async restoreRetry(){try{const saved=JSON.parse(sessionStorage.getItem('so-order-retry')||'null');if(!saved||typeof saved.key!=='string')return;sessionStorage.removeItem('so-order-retry');const fingerprint=typeof saved.body==='string'?await this.fingerprint(saved.body):saved.fingerprint;if(typeof fingerprint==='string'&&/^[a-f0-9]{64}$/.test(fingerprint)){this.retry={fingerprint,key:saved.key};sessionStorage.setItem('so-order-retry',JSON.stringify(this.retry));}}catch{}}

 constructor(){this.http.get<ManualMethod[]>('/api/orders/payment-methods').subscribe({next:methods=>{this.methods.set(methods);this.paymentMethod=methods.find(m=>m.available)?.id||'';},error:()=>this.message.set('לא ניתן לטעון אמצעי תשלום. רעננו ונסו שוב.')});this.cart.refreshCatalog();this.api.refresh().subscribe({next:s=>{if(!this.name)this.name=s.name;if(!this.email)this.email=s.email;},error:()=>this.api.session.set(null)});this.http.get<ShippingSettings>('/api/store/shipping').subscribe({next:s=>this.shipping.set(s),error:()=>this.message.set('לא ניתן לטעון משלוחים. רעננו ונסו שוב.')});this.retryReady=this.restoreRetry();}
 async submit(){if(!normalizePhone(this.phone)){this.message.set('יש להזין מספר טלפון ישראלי תקין.');return;}if(!this.cart.canCheckout()||!this.validAddress()||this.step()!==2||this.busy()||!this.shipping()||!this.cart.cartItems().length||!this.methods().some(m=>m.id===this.paymentMethod&&m.available))return;
 const data={paymentMethod:this.paymentMethod,couponCode:this.couponCode.trim()||null,name:this.name,email:this.email,phone:normalizePhone(this.phone),address:this.address,postalCode:this.pickup?null:this.postalCode.trim()||null,deliveryNotes:this.pickup?null:this.deliveryNotes.trim()||null,pickup:this.pickup,items:this.cart.cartItems().map(i=>({productId:i.product.id,quantity:i.quantity}))};const body=JSON.stringify(data);
 this.busy.set(true);await this.retryReady;let fingerprint:string;try{fingerprint=await this.fingerprint(body);}catch{this.busy.set(false);this.message.set('לא ניתן להכין את ההזמנה בדפדפן הזה. נסו בדפדפן מעודכן.');return;}
 if(!this.retry||this.retry.fingerprint!==fingerprint)this.retry={fingerprint,key:crypto.randomUUID()};try{sessionStorage.setItem('so-order-retry',JSON.stringify(this.retry));}catch{}
 const key=this.retry.key;this.busy.set(true);this.message.set('');this.api.post<Order>('/api/orders',{...data,requestKey:key}).pipe(finalize(()=>this.busy.set(false))).subscribe({next:o=>{this.saved.set(o);this.go(3);try{sessionStorage.removeItem('so-order-retry');}catch{}this.cart.clearCart();},error:e=>{if(e.error?.code==='phone_invalid'){this.go(1);}if(e.error?.code==='stock_unavailable'){this.cart.refreshCatalog();this.quote.set(null);}if(e.error?.code==='coupon_invalid'){this.quote.set(null);this.couponMessage.set(e.error.message);return;}this.message.set(e.status===429?'יש להמתין דקה לפני ניסיון נוסף.':(e.status===0?null:e.error?.message)||'ההזמנה לא אושרה. ניתן לנסות שוב; אותה בקשה לא תיצור הזמנה כפולה.');}});}
}
